/*Funktion för att starta spelet*/
function startGame(){
    const gameScene = "../Gamescene/index.html";
    window.location = gameScene;
}

/*Funktion för att ta sig till information angående kontroler osv */
function controlInfo(){
    const controlScene ="../control.html"
    window.location =controlScene;
}

/*Funktion för att ta sig till The Collection, som innehåller alla programmeringsuppgifter osv */
function goBackToMenu(){
    const theCollection ="../../index.html"
    window.location = theCollection;
}

/*Funktion för att ta sig till kontaktformuläret */
function contact(){
    const contactForm ="https://www.timworksupport.tech/#contact"
    window.location = contactForm;
}